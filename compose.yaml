services:

  family-control:
    build: .
    container_name: family-control
    restart: no

    ports:
      - "8082:8080"

    environment:
      PIHOLE_URL: "${PIHOLE_URL}"
      SCHEDULE_DIR: "/data"
      SCHEDULER_TICK_SECONDS: "30"

    volumes:
      - ./data:/data